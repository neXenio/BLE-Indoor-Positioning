<?xml version="1.0" encoding="utf-8"?>
<menu
        xmlns:android="http://schemas.android.com/apk/res/android">

    <item
            android:id="@+id/menu_value"
            android:title="@string/menu_value">

        <menu>

            <group
                    android:id="@+id/menu_value_group"
                    android:checkableBehavior="single">

                <item
                        android:id="@+id/menu_value_rssi"
                        android:title="@string/menu_value_rssi"
                        android:checked="false"/>

                <item
                        android:id="@+id/menu_value_rssi_filtered"
                        android:title="@string/menu_value_rssi_filtered"
                        android:checked="true"/>

                <item
                        android:id="@+id/menu_value_distance"
                        android:title="@string/menu_value_distance"/>

                <item
                        android:id="@+id/menu_value_frequency"
                        android:title="@string/menu_value_frequency"/>

                <item
                        android:id="@+id/menu_value_variance"
                        android:title="@string/menu_value_variance"/>

            </group>

        </menu>

    </item>

</menu>